extends layout

block content
    .main.container
        .row
            .col-md-6.col-md-offset-3
            form(method='GET' action='/add' name='add')
                select(id='select', name='tableName')
                    each item in list
                        option=item
                input.form-control(type='submit')

